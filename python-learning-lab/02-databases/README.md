# 02 · Databases

Practical projects using PostgreSQL, MongoDB and Redis from Python.

## Modules

| Folder | Description | Status |
|--------|-------------|--------|
| `postgres/` | SQLAlchemy, Alembic migrations, advanced queries | ⏳ Pending |
| `mongodb/` | pymongo, aggregation pipeline, indexes | ⏳ Pending |
| `redis/` | Caching, TTL, pub/sub, rate limiting | ⏳ Pending |

## Prerequisite

Start the databases from the integrator project:

```bash
cd ../06-integrator-project
docker compose up -d
```